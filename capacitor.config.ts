const config = {
  appId: 'com.example.nuxtapp',
  appName: 'NuxtQuasarApp',
  // webDir: '.output/public', // ✅ sửa tại đây
  webDir: '.vercel/output/static', // ✅ sửa tại đây
  bundledWebRuntime: false
}
export default config;
